<%= form_for(@current_exercise) do |f| %>
  <% if @current_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_exercise.errors.count, "error") %> prohibited this current_exercise from being saved:</h2>

      <ul>
      <% @current_exercise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.collection_select :name, options_for_select(@ex_list) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
