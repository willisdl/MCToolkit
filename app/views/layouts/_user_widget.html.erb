<% if mct_signed_in? %>
    <%= current_mct.rank + " " + current_mct.firstname + " " + current_mct.lastname %>
    <%= link_to ' [Logout]', destroy_mct_session_path, :method => :delete %>
<% else %>
    <%= link_to 'Login', new_mct_session_path %>
<% end %>