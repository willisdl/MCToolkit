<!DOCTYPE html>
<html>
<head>
  <title>MCToolkit</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
      <div class="navbar-header">
                    <%= link_to "sample app", '#', id: "logo" %>
      </div>
      <div class="navbar-collapse collapse" id="navbar-main">
        <nav>
          <div class="nav navbar-right">

            <% if mct_signed_in? %>
                <div class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_mct.rank + " " + current_mct.firstname + " " + current_mct.lastname %> <span class="caret"></span></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to 'Logout', destroy_mct_session_path, :method => :delete %></li>
                  </ul>
                </div>


            <% else %>
                <%= link_to 'Login', new_mct_session_path %>
            <% end %>
          </div>
        </nav>
      </div>
  </div>

</div>

<div class="container" style="padding-top: 60px">
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>


</body>
</html>
